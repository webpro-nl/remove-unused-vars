enum Color {
  Red,
  Green,
  Blue,
}

enum Size {
  Small,
  Medium,
  Large,
}

enum Status {
  Active,
  Inactive,
}

const mySize = Size.Medium;
const myStatus = Status.Active;

console.log(mySize, myStatus);

